1. Replace the temporary 4 MiB Batch JSONL per-entry limit by bounding every variable request field and deriving a limit that accounts for JSON escaping and fixed request-envelope overhead.
2. Add deterministic offline tests for api.uploadResumableBytes using a local two-stage std.http.Server fixture that validates upload-start and finalize requests, responses, headers, bodies, and failure paths.
